---
//src/layouts/Layout.astro
/**
 * Componente: Layout Principal
 * Descripción: Define la estructura base del documento HTML, metadatos SEO,
 * precarga de fuentes web y la disposición de componentes globales.
 */
import Footer from '../components/Footer.astro';
import Header from '../components/Header.astro';
import '../styles/main.scss';

export interface Props {
  title: string;
  description?: string;
  image?: string;
  keywords?: string;
  hideFooter?: boolean;
}

const {
  title,
  description = 'Integramos mercados, capital y tecnología para que productores, compradores e inversionistas crezcan con confianza.',
  image = '/Logo_Agrovalue.png',
  keywords = 'agroexportación, agricultura, comercialización agrícola, inversión agrícola, trazabilidad, fruta fresca, logística internacional, Agrovalue',
  hideFooter = false,
} = Astro.props;

// Obtenemos el idioma actual automáticamente (Astro se encarga de saber si estamos en / o en /en/)
const lang = Astro.currentLocale || 'es';

const canonicalURL = new URL(
  Astro.url.pathname,
  Astro.site || 'https://agrovalue.es',
);
const imageUrl = new URL(image, canonicalURL);
---

<!doctype html>
<!-- Asignamos el idioma al HTML -->
<html lang={lang}>
  <head>
    <meta charset='UTF-8' />
    <meta
      name='viewport'
      content='width=device-width, initial-scale=1.0'
    />
    <link
      rel='icon'
      type='image/svg+xml'
      href='/Logo_AgrovalueV.png'
    />
    <meta
      name='generator'
      content={Astro.generator}
    />

    <!-- SEO Básico -->
    <title>{title}</title>
    <meta
      name='description'
      content={description}
    />
    <meta
      name='author'
      content='Cristian Braco'
    />
    <meta
      name='keywords'
      content={keywords}
    />
    <meta
      name='robots'
      content='index, follow'
    />
    <link
      rel='canonical'
      href={canonicalURL}
    />

    <!-- Open Graph / Facebook / LinkedIn -->
    <meta
      property='og:type'
      content='website'
    />
    <meta
      property='og:url'
      content={canonicalURL}
    />
    <meta
      property='og:title'
      content={title}
    />
    <meta
      property='og:description'
      content={description}
    />
    <meta
      property='og:image'
      content={imageUrl}
    />
    <meta
      property='og:site_name'
      content='Agrovalue'
    />
    <!--  Se adapto el locale para SEO -->
    <meta
      property='og:locale'
      content={lang === 'es' ? 'es_ES' : 'en_US'}
    />

    <!-- Twitter -->
    <meta
      property='twitter:card'
      content='summary_large_image'
    />
    <meta
      property='twitter:url'
      content={canonicalURL}
    />
    <meta
      property='twitter:title'
      content={title}
    />
    <meta
      property='twitter:description'
      content={description}
    />
    <meta
      property='twitter:image'
      content={imageUrl}
    />

    <!-- Fuentes Google -->
    <link
      rel='preconnect'
      href='https://fonts.googleapis.com'
    />
    <link
      rel='preconnect'
      href='https://fonts.gstatic.com'
      crossorigin
    />
    <link
      href='https://fonts.googleapis.com/css2?family=DM+Sans:ital,opsz,wght@0,9..40,100..1000;1,9..40,100..1000&family=Space+Grotesk:wght@300..700&display=swap'
      rel='stylesheet'
    />

    <!-- JSON-LD SEO -->
    <script type='application/ld+json'>
      {
        "@context": "https://schema.org",
        "@type": "Organization",
        "name": "Agrovalue",
        "url": "https://www.agrovalue.es",
        "logo": "https://www.agrovalue.es/Logo_Agrovalue.png",
        "description": "Integramos mercados, capital y tecnología para que productores, compradores e inversionistas crezcan con confianza.",
        "contactPoint": {
          "@type": "ContactPoint",
          "email": "contacto@agrovalue.es",
          "contactType": "customer support",
          "availableLanguage": ["es", "en"]
        },
        "sameAs": ["https://www.linkedin.com/company/agrovalue"]
      }
    </script>
  </head>
  <body>
    <div class='app-container'>
      <Header />
      <main class='main-content'>
        <slot />
      </main>
      {!hideFooter && <Footer />}
    </div>
  </body>
</html>
